# v1.1.0 검증 — 2026-09-10

통과:
- index.js와 core.js의 ES 모듈 구문 검사.
- v1 설정 이전 및 원본 보관, OFF 상태 이전.
- 다중 지시문 동시 적용, 같은 깊이·역할의 순서, 개별 OFF, 삭제 후 잔여 주입 제거.
- 20회 반복 호출 시 중복 없음, 다른 확장의 프롬프트 보존.
- Quiet 제외/포함, 전체 OFF 시 개별 상태 유지, JSON 저장값 복원.
- 빈 항목 제외, 깊이 제한, 확장 비활성화, 빈 목록 유지.
- LinkeDOM 시뮬레이션: 마법봉 메뉴 등록, 목록 창 열기/닫기, 입력·추가·토글·이동·복제·삭제·복원, 개별 위치 변경, 제목의 HTML 비실행, 확장 재활성화 시 메뉴 중복 없음.
- ZIP 무결성 및 manifest 참조 파일 존재.

DOM 시뮬레이션은 dialog와 select의 일부 네이티브 동작을 대체했습니다. 실제 브라우저 렌더링·키보드 포커스·실리태번 서버 저장·연결 모델 호출을 검증한 것은 아닙니다.

핵심 검사는 `node test-core.cjs`로 재실행할 수 있습니다.
